---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: connectors.kafka-connect.b1zzu.net
spec:
  group: kafka-connect.b1zzu.net
  names:
    kind: Connector
    listKind: ConnectorList
    plural: connectors
    singular: connector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.connector.state
      name: State
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Connector is the Schema for the connectors API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Connector
            properties:
              cluster:
                description: The name of the Kafka Connect cluster hosting the connector.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              config:
                additionalProperties:
                  type: string
                description: |-
                  Connector configs:
                  Source: https://kafka.apache.org/41/configuration/kafka-connect-configs/#source-connector-configs
                  Sink: https://kafka.apache.org/41/configuration/kafka-connect-configs/#sink-connector-configs
                  MirrorMaker: https://kafka.apache.org/41/configuration/mirrormaker-configs/
                type: object
              exportOffsets:
                description: |-
                  Configuration for exporting offsets to a ConfigMap.
                  This field alone has no effect. To trigger an export,
                  set the annotation kafka-connect.b1zzu.net/offsets: export
                properties:
                  configMapRef:
                    description: Reference to the ConfigMap used for the offset operation.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - configMapRef
                type: object
              importOffsets:
                description: |-
                  Configuration for importing offsets from a ConfigMap.
                  This field alone has no effect. To trigger an import,
                  set the annotation kafka-connect.b1zzu.net/offsets: import
                  The connector must be in the stopped state before importing.
                properties:
                  configMapRef:
                    description: Reference to the ConfigMap used for the offset operation.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - configMapRef
                type: object
              state:
                default: running
                description: 'The desired state of the connector: running, paused,
                  or stopped.'
                enum:
                - running
                - paused
                - stopped
                type: string
            required:
            - cluster
            type: object
          status:
            description: status defines the observed state of Connector
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Connector resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              connector:
                description: connector represents the state of the connector as reported
                  by Kafka Connect.
                properties:
                  state:
                    description: state is the current state of the connector (e.g.,
                      RUNNING, PAUSED, FAILED, STOPPED, UNASSIGNED).
                    type: string
                  trace:
                    description: trace contains the error trace if the connector is
                      in FAILED state.
                    type: string
                  workerID:
                    description: workerID is the Kafka Connect worker that the connector
                      is assigned to.
                    type: string
                required:
                - state
                - workerID
                type: object
              lastExportedOffsetsAt:
                format: date-time
                type: string
              lastImportedOffsetsAt:
                format: date-time
                type: string
              lastResetOffsetsAt:
                format: date-time
                type: string
              lastRestartAt:
                description: lastRestartAt is the timestamp of the last connector
                  or task restart.
                format: date-time
                type: string
              lastStateTransitionAt:
                description: lastStateTransitionAt is the timestamp of the last triggered
                  state transition
                format: date-time
                type: string
              lastUpdatedAt:
                description: lastUpdatedAt is the timestamp of the last update (include
                  when it was created) of the the connector.
                format: date-time
                type: string
              restartCount:
                description: restartCount is the total number of restarts performed
                  on this connector.
                format: int32
                type: integer
              stateTransitionTo:
                description: |-
                  stateTransitionTo is set to the target state when the connector is trying to transition from
                  one state to another, otherwise is nil.
                enum:
                - running
                - paused
                - stopped
                type: string
              tasks:
                description: tasks represents the state of individual connector tasks
                  as reported by Kafka Connect.
                items:
                  description: TaskStateStatus represents the state of a connector
                    task as reported by Kafka Connect.
                  properties:
                    id:
                      description: id is the task identifier.
                      type: integer
                    state:
                      description: state is the current state of the task (e.g., RUNNING,
                        FAILED).
                      type: string
                    trace:
                      description: trace contains the error trace if the task is in
                        FAILED state.
                      type: string
                    workerID:
                      description: workerID is the Kafka Connect worker that the task
                        is assigned to.
                      type: string
                  required:
                  - id
                  - state
                  - workerID
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
