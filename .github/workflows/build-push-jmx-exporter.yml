name: Build and Push JMX Exporter Image

on:
  push:
    branches:
      - main
    paths:
      - 'containers/jmx-exporter/**'
  pull_request:
    paths:
      - 'containers/jmx-exporter/**'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/jmx-exporter

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract JMX Exporter version from Dockerfile
        id: version
        run: |
          version=$(grep -oP 'ARG JMX_EXPORTER_VERSION=\K\S+' containers/jmx-exporter/Dockerfile)
          echo "jmx_exporter_version=$version" >> "$GITHUB_OUTPUT"

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=raw,value=${{ steps.version.outputs.jmx_exporter_version }},enable={{is_default_branch}}

      - name: Build and push JMX Exporter image
        uses: docker/build-push-action@v6
        with:
          context: containers/jmx-exporter
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
